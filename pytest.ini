[pytest]
testpaths = TestCase
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -s --alluredir=allure-results
markers =
    priority(level): 用例优先级，支持 high / medium / low
